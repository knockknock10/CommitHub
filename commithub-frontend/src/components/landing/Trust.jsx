const Trust = () => {
    return (
        <section className="ch-trust">
            <div className="ch-section-inner">
                <p className="ch-trust-label">Building the same way as teams at</p>
                <div className="ch-trust-logos">
                    <span className="ch-trust-logo">stripe</span>
                    <span className="ch-trust-logo">vercel</span>
                    <span className="ch-trust-logo">linear</span>
                    <span className="ch-trust-logo">supabase</span>
                    <span className="ch-trust-logo">railway</span>
                    <span className="ch-trust-logo">planetscale</span>
                    <span className="ch-trust-logo">notion</span>
                    <span className="ch-trust-logo">figma</span>
                </div>
            </div>
        </section>
    );
};

export default Trust;
